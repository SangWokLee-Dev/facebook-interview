package com.facebook.tree;

/** BinaryTree data structure is a tree data structure that has no more than 2 children nodes */
public class BinaryTree {
  Node root;

  /**
   * BinaryTree tree constructor
   *
   * @param data - Root data
   */
  public BinaryTree(int data) {
    this.root = root;
  }

  /** BinaryTree tree constructor data has no root. */
  public BinaryTree() {}
}
